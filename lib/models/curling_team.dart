import 'package:flutter/material.dart';

class CurlingTeam {
  CurlingTeam({
    required this.name,
    required this.color,
    required this.textColor,
    required this.hasHammer,
  });
  String name;
  Color color;
  Color textColor;
  bool hasHammer;
}
